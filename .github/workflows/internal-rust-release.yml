name: internal-rust-release

on:
  workflow_dispatch:
    inputs:
      upstream_tag:
        description: Upstream rust release tag, for example rust-v0.114.0
        required: true
        type: string
      release_ref:
        description: Branch or tag to build from, for example candidate/queue/rust-v0.124.0
        required: true
        type: string
      internal_tag:
        description: Optional internal tag name to publish against
        required: false
        default: ""
        type: string
      publish:
        description: Publish a real GitHub Release instead of a dry-run bundle artifact
        required: false
        default: true
        type: boolean

permissions:
  contents: write

jobs:
  meta:
    runs-on: ubuntu-latest
    outputs:
      internal_tag: ${{ steps.meta.outputs.internal_tag }}
      upstream_tag: ${{ steps.meta.outputs.upstream_tag }}
      version: ${{ steps.meta.outputs.version }}
      publish: ${{ steps.meta.outputs.publish }}
      release_ref: ${{ steps.meta.outputs.release_ref }}
    steps:
      - name: Compute release metadata
        id: meta
        env:
          INPUT_UPSTREAM_TAG: ${{ inputs.upstream_tag }}
          INPUT_RELEASE_REF: ${{ inputs.release_ref }}
          INPUT_INTERNAL_TAG: ${{ inputs.internal_tag }}
          INPUT_PUBLISH: ${{ inputs.publish }}
        shell: bash
        run: |
          set -euo pipefail
          upstream_tag="${INPUT_UPSTREAM_TAG}"
          release_ref="${INPUT_RELEASE_REF}"
          internal_tag="${INPUT_INTERNAL_TAG:-}"
          publish="${INPUT_PUBLISH:-true}"

          if [[ ! "${upstream_tag}" =~ ^rust-v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "unexpected upstream tag format: ${upstream_tag}" >&2
            exit 1
          fi

          if [[ -z "${release_ref}" ]]; then
            echo "release_ref is required" >&2
            exit 1
          fi

          if [[ -z "${internal_tag}" ]]; then
            internal_tag="internal-${upstream_tag}"
          fi

          echo "internal_tag=${internal_tag}" >> "$GITHUB_OUTPUT"
          echo "upstream_tag=${upstream_tag}" >> "$GITHUB_OUTPUT"
          echo "version=${upstream_tag#rust-v}" >> "$GITHUB_OUTPUT"
          echo "publish=${publish}" >> "$GITHUB_OUTPUT"
          echo "release_ref=${release_ref}" >> "$GITHUB_OUTPUT"

  build:
    needs: meta
    name: Build - ${{ matrix.runner }} - ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 360
    defaults:
      run:
        working-directory: codex-rs
    env:
      # macOS release packages archive packed dSYM bundles before stripping.
      CARGO_PROFILE_RELEASE_SPLIT_DEBUGINFO: ${{ contains(matrix.target, 'apple-darwin') && 'packed' || 'off' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            target: x86_64-unknown-linux-musl
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ needs.meta.outputs.release_ref }}

      - name: Install Linux build dependencies
        if: ${{ runner.os == 'Linux' }}
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -y
          packages=(binutils pkg-config libcap-dev)
          if [[ "${{ matrix.target }}" == "x86_64-unknown-linux-musl" ]]; then
            packages+=(libubsan1)
          fi
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends "${packages[@]}"

      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          targets: ${{ matrix.target }}

      - name: Ensure Rust target is installed
        shell: bash
        run: rustup target add "${{ matrix.target }}" --toolchain 1.95.0

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' }}
        name: Use hermetic Cargo home (musl)
        shell: bash
        run: |
          set -euo pipefail
          cargo_home="${GITHUB_WORKSPACE}/.cargo-home"
          mkdir -p "${cargo_home}/bin"
          echo "CARGO_HOME=${cargo_home}" >> "$GITHUB_ENV"
          echo "${cargo_home}/bin" >> "$GITHUB_PATH"
          : > "${cargo_home}/config.toml"

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' }}
        name: Install Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.14.0

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' }}
        name: Install musl build tools
        env:
          TARGET: ${{ matrix.target }}
        run: bash "${GITHUB_WORKSPACE}/.github/scripts/install-musl-build-tools.sh"

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' }}
        name: Configure rustc UBSan wrapper (musl host)
        shell: bash
        run: |
          set -euo pipefail
          ubsan=""
          if command -v ldconfig >/dev/null 2>&1; then
            ubsan="$(ldconfig -p | grep -m1 'libubsan\.so\.1' | sed -E 's/.*=> (.*)$/\1/')"
          fi
          wrapper_root="${RUNNER_TEMP:-/tmp}"
          wrapper="${wrapper_root}/rustc-ubsan-wrapper"
          cat > "${wrapper}" <<EOF
          #!/usr/bin/env bash
          set -euo pipefail
          if [[ -n "${ubsan}" ]]; then
            export LD_PRELOAD="${ubsan}\${LD_PRELOAD:+:\${LD_PRELOAD}}"
          fi
          exec "\$1" "\${@:2}"
          EOF
          chmod +x "${wrapper}"
          echo "RUSTC_WRAPPER=${wrapper}" >> "$GITHUB_ENV"
          echo "RUSTC_WORKSPACE_WRAPPER=" >> "$GITHUB_ENV"

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' }}
        name: Clear sanitizer flags (musl)
        shell: bash
        run: |
          set -euo pipefail
          # Avoid problematic aws-lc jitter entropy code path on musl builders.
          echo "AWS_LC_SYS_NO_JITTER_ENTROPY=1" >> "$GITHUB_ENV"
          target_no_jitter="AWS_LC_SYS_NO_JITTER_ENTROPY_${{ matrix.target }}"
          target_no_jitter="${target_no_jitter//-/_}"
          echo "${target_no_jitter}=1" >> "$GITHUB_ENV"

          echo "RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_ENCODED_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "RUSTDOCFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_BUILD_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_RUSTFLAGS=" >> "$GITHUB_ENV"
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_RUSTFLAGS=" >> "$GITHUB_ENV"

          sanitize_flags() {
            local input="$1"
            input="${input//-fsanitize=undefined/}"
            input="${input//-fno-sanitize-recover=undefined/}"
            input="${input//-fno-sanitize-trap=undefined/}"
            echo "$input"
          }

          cflags="$(sanitize_flags "${CFLAGS-}")"
          cxxflags="$(sanitize_flags "${CXXFLAGS-}")"
          echo "CFLAGS=${cflags}" >> "$GITHUB_ENV"
          echo "CXXFLAGS=${cxxflags}" >> "$GITHUB_ENV"

      - name: Verify Cargo version matches upstream release
        shell: bash
        run: |
          set -euo pipefail
          expected="${{ needs.meta.outputs.version }}"
          actual="$(grep -m1 '^version' Cargo.toml | sed -E 's/version *= *"([^"]+)".*/\1/')"
          [[ "${actual}" == "${expected}" ]] || {
            echo "Cargo.toml version ${actual} does not match ${expected}" >&2
            exit 1
          }

      - if: ${{ matrix.target == 'x86_64-unknown-linux-musl' || matrix.target == 'aarch64-unknown-linux-musl' }}
        name: Configure musl rusty_v8 artifact overrides
        env:
          TARGET: ${{ matrix.target }}
        shell: bash
        run: |
          set -euo pipefail
          version="$(python3 "${GITHUB_WORKSPACE}/.github/scripts/rusty_v8_bazel.py" resolved-v8-crate-version)"
          release_tag="rusty-v8-v${version}"
          base_url="https://github.com/openai/codex/releases/download/${release_tag}"
          archive="https://github.com/openai/codex/releases/download/rusty-v8-v${version}/librusty_v8_release_${TARGET}.a.gz"
          binding_dir="${RUNNER_TEMP}/rusty_v8"
          binding_path="${binding_dir}/src_binding_release_${TARGET}.rs"
          mkdir -p "${binding_dir}"
          curl -fsSL "${base_url}/src_binding_release_${TARGET}.rs" -o "${binding_path}"
          echo "RUSTY_V8_ARCHIVE=${archive}" >> "$GITHUB_ENV"
          echo "RUSTY_V8_SRC_BINDING_PATH=${binding_path}" >> "$GITHUB_ENV"

      - name: Build release binaries
        shell: bash
        run: |
          set -euo pipefail
          target="${{ matrix.target }}"
          if [[ "${target}" == *"-linux-"* ]]; then
            # Keep the hosted Linux runners under their memory ceiling during late release linking.
            export CARGO_PROFILE_RELEASE_LTO=false
            export CARGO_PROFILE_RELEASE_CODEGEN_UNITS=16
            cargo build --locked --target "${target}" --release --jobs 1 --bin codex-responses-api-proxy
            cargo build --locked --target "${target}" --release --jobs 1 --bin codex
            cargo build --locked --target "${target}" --release --jobs 1 --bin codex-code-mode-host
          else
            cargo build --locked --target "${target}" --release --bin codex --bin codex-code-mode-host --bin codex-responses-api-proxy
          fi

      - name: Stage internal release assets
        shell: bash
        run: |
          set -euo pipefail
          release_dir="target/${{ matrix.target }}/release"
          dest="dist/${{ matrix.target }}"
          mkdir -p "${dest}"

          bash "${GITHUB_WORKSPACE}/.github/scripts/archive-release-symbols-and-strip-binaries.sh" \
            --target "${{ matrix.target }}" \
            --artifact-name "${{ matrix.target }}" \
            --release-dir "${release_dir}" \
            --archive-dir "${dest}" \
            --binaries "codex codex-code-mode-host codex-responses-api-proxy"

          cp "${release_dir}/codex" "${dest}/codex"
          cp "${release_dir}/codex-code-mode-host" "${dest}/codex-code-mode-host"
          cp "${release_dir}/codex-responses-api-proxy" "${dest}/codex-responses-api-proxy"

          tar -C "${dest}" -czf "${dest}/codex-${{ matrix.target }}.tar.gz" codex codex-code-mode-host
          tar -C "${dest}" -czf "${dest}/codex-responses-api-proxy-${{ matrix.target }}.tar.gz" codex-responses-api-proxy

          rm "${dest}/codex" "${dest}/codex-code-mode-host" "${dest}/codex-responses-api-proxy"

      - uses: actions/upload-artifact@v7
        with:
          name: internal-release-${{ matrix.target }}
          path: codex-rs/dist/${{ matrix.target }}/*

  bundle:
    needs:
      - meta
      - build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ needs.meta.outputs.release_ref }}

      - uses: actions/download-artifact@v8
        with:
          path: dist

      - name: Replace musl release assets
        if: ${{ needs.meta.outputs.publish == 'true' }}
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          asset_dir="dist/internal-release-x86_64-unknown-linux-musl"
          gh release upload "${{ needs.meta.outputs.internal_tag }}" \
            "${asset_dir}/codex-x86_64-unknown-linux-musl.tar.gz" \
            "${asset_dir}/codex-responses-api-proxy-x86_64-unknown-linux-musl.tar.gz" \
            "${asset_dir}/codex-symbols-x86_64-unknown-linux-musl.tar.gz" \
            --clobber \
            --repo "${GITHUB_REPOSITORY}"

      - name: Upload dry-run bundle
        if: ${{ needs.meta.outputs.publish != 'true' }}
        uses: actions/upload-artifact@v7
        with:
          name: internal-release-dry-run-${{ needs.meta.outputs.version }}
          path: dist/**
